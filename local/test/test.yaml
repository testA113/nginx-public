apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-data
  namespace: test
  labels:
    app: test
    managed-by: portainer-run
  annotations:
    portainer-run/deploy-type: vibe
    portainer-run/git-target-id: a0df36e5-291f-489a-a96e-ad76eb89949f
    portainer-run/git-branch: main
    portainer-run/git-path: local/test/test.yaml
    portainer-run/vibe-source-path: '1'
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
  namespace: test
  labels:
    app: test
    managed-by: portainer-run
  annotations:
    portainer-run/deploy-type: vibe
    portainer-run/git-target-id: a0df36e5-291f-489a-a96e-ad76eb89949f
    portainer-run/git-branch: main
    portainer-run/git-path: local/test/test.yaml
    portainer-run/vibe-source-path: '1'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
        managed-by: portainer-run
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      automountServiceAccountToken: false
      initContainers:
        - name: vibe-sync
          image: alpine/git:latest
          command:
            - sh
            - '-c'
            - >-
              git config --global http.sslVerify false && git clone --depth 1 --branch main
              https://${GIT_USERNAME}:${GIT_TOKEN}@github.com/testa113/nginx-public /tmp/repo && mkdir -p
              /usr/share/nginx/html && cp -r /tmp/repo/1/. /usr/share/nginx/html/ && rm -rf /tmp/repo
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 512Mi
          volumeMounts:
            - name: app-data
              mountPath: /usr/share/nginx/html
          env:
            - name: GIT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: test-git-credentials
                  key: username
            - name: GIT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: test-git-credentials
                  key: token
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      containers:
        - name: test
          image: nginxinc/nginx-unprivileged:alpine
          command:
            - sh
            - '-c'
            - nginx -g 'daemon off;'
          workingDir: /usr/share/nginx/html
          ports:
            - containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: app-data
              mountPath: /usr/share/nginx/html
          resources:
            requests:
              cpu: 100m
              memory: 1Gi
            limits:
              cpu: '1'
              memory: 4Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: app-data
          persistentVolumeClaim:
            claimName: test-data
---
apiVersion: v1
kind: Service
metadata:
  name: test
  namespace: test
  labels:
    app: test
    managed-by: portainer-run
  annotations:
    portainer-run/deploy-type: vibe
    portainer-run/git-target-id: a0df36e5-291f-489a-a96e-ad76eb89949f
    portainer-run/git-branch: main
    portainer-run/git-path: local/test/test.yaml
    portainer-run/vibe-source-path: '1'
spec:
  type: NodePort
  selector:
    app: test
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
